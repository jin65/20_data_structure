히프
>>우선순위 큐를 구현할때 자주 사용

최대 트리(max tree) : 각 노드의 키값이 그 자식의 키 값보다 작지 않은 트리(부모노드 키값>=자식노드키값)>> 루트노드가 값이 제일 크다
최대 히프: 최대 트리이면서 완전이진트리

****트리에서 부모노드의 인덱스==자식노드인덱스/2

히프에서 인덱스 0 >> 공백으로 비워둔다(계산의 편의를 위해)
∵부모노드의 인덱스를 찾을때 자식노드/2로 찾는데 부모노드가 인덱스가 0이라면 자식노드를 /2했을때 0이 나오는 인덱스가 존재할 수 없기때문에 인덱스 1부터 사용한다.