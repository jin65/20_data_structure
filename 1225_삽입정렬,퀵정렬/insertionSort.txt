insertion sort
삽입정렬
정렬된 i개레코드의 리스트에다가 element e를 집어넣는데
이때 a[0]를 삽입할 e로 지정을 한 후, 정렬된 리스트의 제일 끝에서부터 [0]방향으로 하나씩 옮겨가면서 e.key와 비교를 한다. 그래서 만약 e가 a[i]보다 크다면 e는 a[i+1]번째에 삽입이 된다.

전체 리스트에서 보자면 선택된 값보다 작은 곳에 있는 레코드들의 크기를 비교해 선택된 값을 삽입할 위치를 정해서 정렬한다.
